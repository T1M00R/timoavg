/** @type {import('next').NextConfig} */
const nextConfig = {
  webpack: (config) => {
    config.resolve.fallback = {
      ...config.resolve.fallback,
      fs: false,
      path: false,
      crypto: false,
    };
    return config;
  },
  async headers() {
    return [
      {
        source: "/:path*",
        headers: [
          {
            key: "Cross-Origin-Embedder-Policy",
            value: "require-corp",
          },
          {
            key: "Cross-Origin-Opener-Policy",
            value: "same-origin",
          },
        ],
      },
    ];
  },
  // Add static file serving configuration
  async rewrites() {
    return [
      {
        source: '/node_modules/@ffmpeg/core/dist/:path*',
        destination: '/_next/static/ffmpeg/:path*',
      },
    ];
  },
}

module.exports = nextConfig; 